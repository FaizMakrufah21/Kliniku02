name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.2.0'
      SERVERPOD_PASSWORD_database: 'VCTEO7Db9GjH3ZT3A9kZI8_mORHMry_u'
      SERVERPOD_PASSWORD_redis: 'EVI_I9JiothjZkhogFol_LewHcXGPWtn'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: kliniku_server_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: kliniku_server_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: kliniku_server_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: kliniku_server_server
        run: serverpod generate

      - name: Run tests
        working-directory: kliniku_server_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: kliniku_server_server
        run: docker compose down -v
